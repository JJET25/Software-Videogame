const THE_SWARM = {
  name: "the_swarm",
  swarm: 6,
  tank: 0,
  ranged: 0,
};

const THE_SIEGE = {
  name: "the_siege",
  swarm: 2,
  tank: 1,
  ranged: 1,
};

const SNIPER_NEST = {
  name: "sniper_nest",
  swarm: 2,
  tank: 0,
  ranged: 3,
};

const THE_BRUTE = {
  name: "the_brute",
  swarm: 1,
  tank: 2,
  ranged: 0,
};

const CROSSFIRE = {
  name: "crossfire",
  swarm: 0,
  tank: 1,
  ranged: 3,
};

const ELITE_GUARD = {
  name: "elite_guard",
  swarm: 1,
  tank: 1,
  ranged: 2,
};

export const ENCOUNTER_TEMPLATES = [
  THE_SWARM,
  THE_SIEGE,
  SNIPER_NEST,
  THE_BRUTE,
  CROSSFIRE,
  ELITE_GUARD,
];
